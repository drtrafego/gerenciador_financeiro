// Layout transparente — a sidebar global está no (dashboard)/layout.tsx
export default function DashboardSectionLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
